<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>라면을 끓이는 순서</title>
    <script>
        function addlist(){
            let ui = prompt("라면을 끓이는 순서를 입력하라"); //프롬프트창 출력
            ol = document.getElementById("ol"); //id로 불러오기
            let li = document.createElement("li"); //li요소 생성
            li.innerHTML = ui; //프롬프트로 입력받은 내용을 li에 채우기
            ol.appendChild(li); //ol에 li추가
        }
    </script>
</head>
<body>
    <h3>라면을 끓이는 순서</h3>
    <hr>
    <ol id="ol" onclick=addlist() type="A" style="background-color:yellowgreen">
        <li>물을 끓인다.</li>
    </ol>
</body>
</html>
